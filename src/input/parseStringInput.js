import yaml from "js-yaml";

/**
 * Parses the input song data.
 * 
 * @param {any} songData - The raw song data to be parsed.
 * @returns {any} The parsed song data (currently returned as-is).
 */
function parseSongInput(songData) {
  return songData;
}

export default parseSongInput;
